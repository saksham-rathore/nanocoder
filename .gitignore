# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Instrumented libs
lib-cov

# Coverage
coverage
*.lcov
.nyc_output

# Grunt intermediate storage
.grunt

# Dependency directories
node_modules/
# Also match a bare `node_modules` entry: the trailing-slash form above only
# matches directories, so a stray symlink of that name would slip through.
node_modules
jspm_packages/
bower_components/
web_modules/

# Build output
build/Release
dist/
out
.next
.nuxt
public
result
result-*

# TypeScript cache
*.tsbuildinfo

# Optional caches
.npm
.eslintcache
.stylelintcache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Parcel/Snowpack/Gatsby/Storybook cache
.cache
.parcel-cache
.storybook-out
storybook-static
.out

# Temporary folders
.tmp/
tmp/
temp/
.test-temp/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Package manager lock files (keep pnpm-lock.yaml)
package-lock.json
yarn.lock

# Agents configuration
agents.config.json
.claude
agents

# Nanocoder checkpoints (user data)
# These contain conversation snapshots and should not be committed
**/checkpoints/

# Action timeline before-images (per-session user data)
.nanocoder/timeline/

# Heap snapshots
.heap-snapshots

coverage

.nanocoder/tasks.json
.nanocoder/schedule-runs.json
.nanocoder/schedules.json
.generated-prompts/
.nanocoder/skills

# Generated quality report (baseline.json is checked in, others are not)
benchmarks/report.json
benchmarks/explain.json
benchmarks/.module-*
.github/issues/docs

# Auto-generated CSS
plugins/vscode/media/chat-panel.css
assets/*.vsix

# nc-review scratch: PR context and verdict, written into the workspace
# because Nanocoder file tools are sandboxed to the project directory.
.nc-review/
